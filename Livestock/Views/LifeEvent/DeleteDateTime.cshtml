@model Website.Models.LifeEventEditDateTime
@{
    ViewData["Title"] = "DeleteDateTime";
}

<h2>Are you sure you want to delete this DateTime event?</h2>

<div class="col-md-4">
    <dl>
        <dt>@Html.LabelFor(m => Model.Common.Description)</dt>
        <dd>@Html.DisplayFor(m => Model.Common.Description)</dd>

        <dt>@Html.LabelFor(m => Model.DateTime)</dt>
        <dd>@Html.DisplayFor(m => Model.DateTime)</dd>
    </dl>

    <form class="ui form"asp-action="DeleteDateTimePost">
        <input type="hidden" asp-for="Common.Id" />
        <input type="submit" value="Delete Event" class="btn btn-danger" /> |
        <a asp-action="Edit" asp-route-id="@(Model.Common.Id)">Back to Critter</a>
    </form>
</div>